# Routines

- `maintain.prompt.md`: 定期Routine（推奨: 週次〜隔週）からこのRepositoryを対象に実行するprompt。scheduleはRoutine側が持つ。
- 実行環境の前提: `git submodule update --init --recursive`が可能（`shared-agent-plugins`の閲覧権限）、Python 3.11+、共有資産releaseまで行う場合はNode.js 24とGitHubへのpush権限。
